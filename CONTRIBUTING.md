# Contributing

Use Node.js 22 or newer.

```bash
npm install
npm run check
npm test
```

Keep the default matching narrow. New error patterns should include a concrete
Antigravity example and should not make ordinary Retry buttons eligible.

Do not test startup changes against a real installation unless you intend to
replace it. The service renderers can be tested without registering anything.
